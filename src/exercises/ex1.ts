export const meta = {
  id: 'ex1',
  title: 'Sumar dos números',
  description: 'Implementa la función `sum(a, b)` que devuelve la suma de dos números.',
}

// TODO: implementa esta función para resolver el ejercicio
export function sum(a: number, b: number) {
  return a + b
}

export default { meta, sum }
